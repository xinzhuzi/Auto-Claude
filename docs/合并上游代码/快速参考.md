# 快速参考（2026-02-28）

## 一次性执行主流程

```bash
cd /Users/zhengbingjin/Project/Github/Auto-Claude
git branch backup/pre-merge-20260228
git remote remove local-upstream 2>/dev/null || true
git remote add local-upstream /Users/zhengbingjin/Project/temp/Auto-Claude
git fetch local-upstream
git merge local-upstream/develop --no-commit
```

## 仅查看待合并提交

```bash
git log --oneline develop..local-upstream/develop
```

## 仅查看待合并文件

```bash
git diff --name-status develop local-upstream/develop
```

## 冲突处理常用命令

```bash
git status
git show :2:CLAUDE.md > /tmp/claude.ours.md
git show :3:CLAUDE.md > /tmp/claude.theirs.md
git add CLAUDE.md
```

## 提交与推送

```bash
git commit -m "merge: sync local-upstream/develop and resolve CLAUDE.md"
git push origin develop
```
