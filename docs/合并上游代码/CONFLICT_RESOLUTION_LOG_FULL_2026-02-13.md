# 冲突处理记录（完整版｜融合策略）
生成时间：2026-02-14 07:24:36
说明：以下条目均以“融合”为默认策略，兼顾上游新增与本地定制。

---
## `apps/backend/agents/coder.py`
**选择**：`融合`

**原因**：
- 保留上游执行链路改进 + 本地定制流程

**差异统计**：
- 上游 vs 本地 约 +35 / -636 行

**上游新增类/函数**：_check_and_clear_resume_file, _reset_concurrency_state, parse_rate_limit_reset_time, validate_subtask_files

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 任务执行、计划续接、输出格式

**处理人/时间**：
- （待填写）

---
## `apps/backend/agents/session.py`
**选择**：`融合`

**原因**：
- 保留上游执行链路改进 + 本地定制流程

**差异统计**：
- 上游 vs 本地 约 +39 / -183 行

**上游新增类/函数**：_execute_recovery_action

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 任务执行、计划续接、输出格式

**处理人/时间**：
- （待填写）

---
## `apps/backend/core/git_executable.py`
**选择**：`融合`

**原因**：
- 核心稳定性优先，兼顾本地功能扩展

**差异统计**：
- 上游 vs 本地 约 +56 / -5 行

**本地新增类/函数**：_ensure_common_paths_in_env

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 登录鉴权、工作树创建/删除

**处理人/时间**：
- （待填写）

---
## `apps/backend/runners/insights_runner.py`
**选择**：`融合`

**原因**：
- 集成流程敏感，需保留双方功能

**差异统计**：
- 上游 vs 本地 约 +18 / -6 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- PR/MR 流程、限流恢复

**处理人/时间**：
- （待填写）

---
## `apps/frontend/package.json`
**选择**：`融合`

**原因**：
- 依赖需对齐上游并保留本地新增

**差异统计**：
- 上游 vs 本地 约 +48 / -47 行

**上游新增依赖/脚本**：dependencies:rehype-raw, dependencies:rehype-sanitize, dependencies:xstate, scripts:test:verify-linux, scripts:verify:linux
**本地新增依赖/脚本**：dependencies:driver.js, dependencies:react-resizable-panels, dependencies:reactflow

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 构建/启动

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/agent/agent-manager.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +92 / -219 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/claude-profile/usage-monitor.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +15 / -61 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/index.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +24 / -155 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/ipc-handlers/agent-events-handlers.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +293 / -171 行

**本地新增声明**：ExecutionPhase, validateStatusTransition

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/ipc-handlers/mcp-handlers.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +366 / -52 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/ipc-handlers/task/execution-handlers.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +306 / -276 行

**本地新增声明**：atomicWriteFileSync

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/main/project-initializer.ts`
**选择**：`融合`

**原因**：
- 主流程与 IPC 核心，需保留双方能力

**差异统计**：
- 上游 vs 本地 约 +25 / -30 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 启动、任务流转、IPC

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/preload/api/task-api.ts`
**选择**：`融合`

**原因**：
- IPC 契约层变更需双向兼容

**差异统计**：
- 上游 vs 本地 约 +15 / -52 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- IPC 调用

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/renderer/App.tsx`
**选择**：`融合`

**原因**：
- UI 定制与上游交互需共存

**差异统计**：
- 上游 vs 本地 约 +54 / -106 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 页面交互与状态更新

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/renderer/components/TerminalGrid.tsx`
**选择**：`融合`

**原因**：
- UI 定制与上游交互需共存

**差异统计**：
- 上游 vs 本地 约 +113 / -261 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 页面交互与状态更新

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/renderer/components/settings/AppSettings.tsx`
**选择**：`融合`

**原因**：
- UI 定制与上游交互需共存

**差异统计**：
- 上游 vs 本地 约 +61 / -10 行

**本地新增声明**：SupportSection

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 页面交互与状态更新

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/renderer/components/task-detail/hooks/useTaskDetail.ts`
**选择**：`融合`

**原因**：
- UI 定制与上游交互需共存

**差异统计**：
- 上游 vs 本地 约 +67 / -71 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 页面交互与状态更新

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/renderer/components/terminal/useXterm.ts`
**选择**：`融合`

**原因**：
- UI 定制与上游交互需共存

**差异统计**：
- 上游 vs 本地 约 +95 / -228 行

**上游新增声明**：UseXtermReturn
**本地新增声明**：Navigator, NavigatorUAData, debounce

**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 页面交互与状态更新

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/shared/constants/ipc.ts`
**选择**：`融合`

**原因**：
- 共享类型与常量需保持一致

**差异统计**：
- 上游 vs 本地 约 +83 / -27 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 类型检查与编译

**处理人/时间**：
- （待填写）

---
## `apps/frontend/src/shared/types/index.ts`
**选择**：`融合`

**原因**：
- 共享类型与常量需保持一致

**差异统计**：
- 上游 vs 本地 约 +1 / -4 行


**关键改动点**：
- 合并上游新增能力并接入本地流程
- 避免删改核心路径或削弱本地扩展

**风险点**：
- 逻辑分歧导致行为不一致
- 上游修复被本地覆盖造成回退

**验证项**：
- 类型检查与编译

**处理人/时间**：
- （待填写）
