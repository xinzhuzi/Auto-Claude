# 验证清单（本轮）

## A. Git 状态

```bash
git status
```

通过标准：
- 无 `both modified` 冲突残留
- 仅包含预期变更

## B. 关键文件内容

```bash
rg -n "Work Approach: Orchestrator-First|Context Management" CLAUDE.md
rg -n "claude-agent-sdk|react-i18next|develop" CLAUDE.md
```

通过标准：
- 上游新增结构可见
- 基础关键规则未丢失

## C. 提交核对

```bash
git log --oneline -n 3
git show --name-status --oneline HEAD
```

通过标准：
- 最新提交为本次 merge 提交
- 变更文件与预期一致（核心为 `CLAUDE.md`）
