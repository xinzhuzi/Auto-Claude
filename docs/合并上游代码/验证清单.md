# 合并验证清单

> 合并完成后的验证步骤

---

## 1. 代码完整性验证

### 1.1 冲突标记检查

```bash
cd /Users/zhengbingjin/Project/Github/Auto-Claude

# 检查是否有残留的冲突标记
echo "检查冲突标记..."
grep -r "<<<<<<" apps/ --include="*.ts" --include="*.tsx" --include="*.json" && echo "发现冲突标记!" || echo "✓ 无冲突标记"
grep -r ">>>>>>" apps/ --include="*.ts" --include="*.tsx" --include="*.json" && echo "发现冲突标记!" || echo "✓ 无冲突标记"
grep -r "======" apps/ --include="*.ts" --include="*.tsx" --include="*.json" && echo "发现冲突标记!" || echo "✓ 无冲突标记"
```

### 1.2 TypeScript 编译检查

```bash
cd apps/frontend

# 类型检查
npm run typecheck 2>&1 | head -50

# 应该没有错误
```

### 1.3 依赖检查

```bash
cd apps/frontend

# 检查缺失依赖
npm ls 2>&1 | grep "missing" && echo "有缺失依赖" || echo "✓ 依赖完整"

# 检查 peer 依赖
npm ls 2>&1 | grep "peer dep" && echo "有 peer 依赖问题" || echo "✓ peer 依赖正常"
```

---

## 2. 构建验证

### 2.1 前端构建

```bash
cd apps/frontend

# 清理并构建
rm -rf dist/
npm run build

# 检查构建输出
ls -la dist/
echo "✓ 前端构建完成"
```

### 2.2 后端检查

```bash
cd apps/backend

# 检查 Python 语法
python -m py_compile $(find . -name "*.py") 2>&1 | head -20

# 应该没有语法错误
echo "✓ 后端语法检查完成"
```

---

## 3. 功能验证

### 3.1 核心功能清单

| 功能 | 验证方法 | 状态 |
|------|----------|------|
| **任务管理** | 创建、执行、停止任务 | [ ] |
| **终端** | 打开、输入、关闭 | [ ] |
| **PR Review** | 打开 PR，查看差异 | [ ] |
| **Roadmap** | 创建、编辑路线图 | [ ] |

### 3.2 本地独有功能清单

| 功能 | 验证方法 | 状态 |
|------|----------|------|
| **工作流工作室** | 打开 /workflow 路由 | [ ] |
| **小说工作室** | 打开 /novel 路由 | [ ] |
| **Code Server** | 启动 Code Server | [ ] |
| **道劫设置** | 打开设置 → 道劫 | [ ] |
| **技能模块** | 验证技能相关功能 | [ ] |

### 3.3 XState 功能验证

| 状态机 | 验证方法 | 状态 |
|--------|----------|------|
| **task-machine** | 执行任务，观察状态转换 | [ ] |
| **terminal-machine** | 操作终端，观察状态 | [ ] |
| **pr-review-machine** | 进行 PR 审查 | [ ] |
| **roadmap-generation-machine** | 生成路线图 | [ ] |

---

## 4. 运行验证

### 4.1 启动应用

```bash
# 终端 1: 启动后端
cd /Users/zhengbingjin/Project/Github/Auto-Claude
python run.py

# 终端 2: 启动前端
cd /Users/zhengbingjin/Project/Github/Auto-Claude/apps/frontend
npm run dev
```

### 4.2 基本操作测试

```
1. [ ] 应用正常启动
2. [ ] 可以创建项目
3. [ ] 可以创建任务
4. [ ] 可以执行任务
5. [ ] 终端正常工作
6. [ ] 可以切换项目
7. [ ] 设置可以保存
```

### 4.3 本地功能测试

```
1. [ ] 工作流工作室可以打开
2. [ ] 小说工作室可以打开
3. [ ] 道劫设置显示正确
4. [ ] 本地菜单项显示正确
```

---

## 5. 回归测试

### 5.1 上游 Bug 修复验证

| PR | Bug | 验证方法 | 状态 |
|----|-----|----------|------|
| #1853 | QA validation 卡住 | 执行 QA 流程 | [ ] |
| #1847 | Spec path crash | 加载 spec | [ ] |
| #1836 | Terminal blank | 切换项目时终端 | [ ] |
| #1833 | Kanban 卡住 | 操作 Kanban | [ ] |

### 5.2 原有功能验证

```
1. [ ] 任务创建流程正常
2. [ ] 任务执行流程正常
3. [ ] 终端会话正常
4. [ ] PR 创建正常
5. [ ] 文件操作正常
```

---

## 6. 性能验证

### 6.1 启动时间

```bash
# 测量启动时间
time npm run dev

# 记录: _____ 秒
```

### 6.2 内存使用

```bash
# macOS
ps aux | grep -i electron

# 记录内存使用: _____ MB
```

---

## 7. 打包验证

### 7.1 Windows 打包

```bash
cd apps/frontend
npm run build:win

# 检查输出
ls -la release/
```

### 7.2 macOS 打包

```bash
cd apps/frontend
npm run build:mac

# 检查输出
ls -la release/
```

---

## 8. 文档验证

### 8.1 本地文档完整性

```bash
cd /Users/zhengbingjin/Project/Github/Auto-Claude/docs

# 检查本地文档是否存在
ls -la 中文/ 道劫/ 小说模块/ cc-wf-studio/
```

### 8.2 文档列表

| 目录 | 状态 |
|------|------|
| 中文/ | [ ] |
| 道劫/ | [ ] |
| 小说模块/ | [ ] |
| cc-wf-studio/ | [ ] |
| 编辑器/ | [ ] |
| 打包/ | [ ] |
| 代理终端/ | [ ] |
| 任务/ | [ ] |
| Coding/ | [ ] |
| Planning/ | [ ] |
| Validation/ | [ ] |
| git/ | [ ] |
| hook/ | [ ] |
| App名字/ | [ ] |

---

## 9. 最终确认

### 9.1 Git 状态

```bash
git status

# 应该显示:
# nothing to commit, working tree clean
```

### 9.2 签字确认

```
验证人: _______________
验证日期: _______________
版本: _______________
```

---

## 10. 问题记录

| 问题 | 解决方案 | 状态 |
|------|----------|------|
| | | |
| | | |
| | | |
