# Auto-Claude 中文翻译完整指南

> **文档版本**: 3.4
> **更新日期**: 2025-01-27
> **更新内容**: 
> - 添加配置文件修改说明（i18n.ts、index.ts）
> - 添加上游同步完整检查清单
> - 添加本地翻译提交记录
> - 移除 Toolbar.tsx 和 LoadWorkflowDialog.tsx 硬编码中文，使用翻译系统
> **适用版本**: Auto-Claude v2.7.5+
> **目的**: 记录翻译架构、代码逻辑和上游同步流程

---

## 目录

1. [翻译架构概览](#1-翻译架构概览)
2. [CC-WF-Studio 翻译系统](#2-cc-wf-studio-翻译系统)
3. [上游同步翻译流程](#3-上游同步翻译流程)
4. [翻译代码规范](#4-翻译代码规范)
5. [质量保证](#5-质量保证)
6. [维护指南](#6-维护指南)
7. [常见问题](#7-常见问题)
8. [附录](#附录)

---

## 1. 翻译架构概览

### 1.1 整体架构

Auto-Claude 使用 **react-i18next** 框架实现国际化，采用**分离式翻译**架构：

```
Auto-Claude 翻译系统
├── 主应用翻译 (apps/frontend/src/shared/i18n/)
│   ├── 命名空间: common, navigation, settings, tasks 等
│   └── 用途: 主应用界面翻译
└── CC-WF-Studio 翻译 (apps/cc-wf-studio/src/i18n/)
    ├── 命名空间: ccwfstudio
    └── 用途: 工作流编辑器界面翻译
```

### 1.2 工作成果统计

| 指标 | 数值 |
|------|------|
| **翻译文件** | 18 个 JSON 文件 |
| **翻译键总数** | ~1700 个 |
| **支持语言** | 6 种 |
| **完成度** | 100% |

### 1.3 目录结构

```
apps/frontend/src/shared/i18n/
├── index.ts                    # 主应用 i18n 配置
└── locales/
    ├── en/                     # 英文翻译
    │   ├── common.json
    │   ├── navigation.json
    │   ├── settings.json
    │   ├── ... (11 个文件)
    │   └── workflowStudio.json  # 已废弃，使用 cc-wf-studio 的翻译
    ├── fr/                     # 法文翻译
    └── zh-CN/                  # 简体中文翻译

apps/cc-wf-studio/src/i18n/
├── index.ts                    # CC-WF-Studio i18n 导出
└── locales/
    ├── en/
    │   └── workflowStudio.json # 英文翻译
    ├── fr/
    │   └── workflowStudio.json # 法文翻译
    ├── zh-CN/
    │   └── workflowStudio.json # 简体中文翻译
    ├── ja/
    │   └── workflowStudio.json # 日语翻译
    ├── ko/
    │   └── workflowStudio.json # 韩语翻译
    └── zh-TW/
        └── workflowStudio.json # 繁体中文翻译
```

### 1.4 性能影响

| 指标 | 影响 |
|------|------|
| **文件加载** | 最小 - 仅在初始化时加载一次 |
| **语言切换** | 即时（<100ms） |
| **内存占用** | 增加 ~1-2MB |
| **构建体积** | 增加 ~30-40KB（gzip 压缩后） |

---

## 2. CC-WF-Studio 翻译系统

### 2.1 翻译文件位置

CC-WF-Studio 的翻译文件位于 `apps/cc-wf-studio/src/i18n/locales/` 目录下：

| 语言 | 目录 | 文件 | 状态 |
|------|------|------|------|
| English | `en/` | workflowStudio.json | ✅ 基准 |
| French | `fr/` | workflowStudio.json | ✅ 完成 |
| 简体中文 | `zh-CN/` | workflowStudio.json | ✅ 完成 |
| 日本語 | `ja/` | workflowStudio.json | ✅ 完成 |
| 한국어 | `ko/` | workflowStudio.json | ✅ 完成 |
| 繁體中文 | `zh-TW/` | workflowStudio.json | ✅ 完成 |

### 2.2 翻译文件内容结构

`workflowStudio.json` 包含以下翻译键：

```json
{
  "loading": "正在加载工具架构...",
  "toolbar": {
    "selectProject": "选择项目",
    "projectSelected": "项目已选择",
    "save": "保存",
    "saving": "保存中...",
    "load": "加载",
    "export": "导出",
    ...
  },
  "workflowName": {
    "placeholder": "工作流名称",
    "required": "工作流名称不能为空",
    ...
  },
  ...
}
```

### 2.3 在组件中使用翻译

#### 步骤 1: 导入 useTranslation

```typescript
import { useTranslation } from 'react-i18next';
```

#### 步骤 2: 使用 ccwfstudio 命名空间

```typescript
export const Toolbar: React.FC = () => {
  // 使用 ccwfstudio 命名空间
  const { t } = useTranslation('ccwfstudio');

  // 使用翻译
  return (
    <Button>
      {t('toolbar.save')}
    </Button>
  );
};
```

#### 步骤 3: 带变量的翻译

```typescript
// 翻译文件: "currentProject": "当前项目: {{path}}"
const description = t('toolbar.currentProject', { path: result.data });
// 输出: "当前项目: /Users/user/project"
```

### 2.4 CC-WF-Studio 组件翻译清单

需要使用翻译的组件：

| 组件 | 文件路径 | 翻译键前缀 |
|------|---------|-----------|
| Toolbar | `components/toolbar/Toolbar.tsx` | `toolbar.*` |
| EditableNameField | `components/toolbar/EditableNameField.tsx` | `workflowName.*` |
| LoadWorkflowDialog | `components/dialogs/LoadWorkflowDialog.tsx` | `loadDialog.*` |
| WorkflowCanvas | `components/canvas/WorkflowCanvas.tsx` | `canvas.*` |
| NodePalette | `components/canvas/NodePalette.tsx` | `palette.*` |
| PropertyPanel | `components/canvas/PropertyPanel.tsx` | `property.*` |
| PromptNode | `components/nodes/PromptNode.tsx` | `nodes.prompt.*` |
| SubAgentNode | `components/nodes/SubAgentNode.tsx` | `nodes.subAgent.*` |
| McpNode | `components/nodes/McpNode.tsx` | `nodes.mcp.*` |
| AskUserQuestionNode | `components/nodes/AskUserQuestionNode.tsx` | `nodes.askUserQuestion.*` |
| IfElseNode | `components/nodes/IfElseNode.tsx` | `nodes.ifElse.*` |
| SwitchNode | `components/nodes/SwitchNode.tsx` | `nodes.switch.*` |

### 2.5 翻译键组织规范

```json
{
  "toolbar": {
    "selectProject": "选择项目",
    "projectSelected": "项目已选择",
    "currentProject": "当前项目: {{path}}",
    "save": "保存",
    "saving": "保存中...",
    "load": "加载",
    "loading": "加载中...",
    "export": "导出",
    "exporting": "导出中..."
  },
  "workflowName": {
    "placeholder": "workflow-name",
    "required": "工作流名称不能为空",
    "invalidFormat": "工作流名称格式无效",
    "patternError": "只允许小写字母、数字、连字符和下划线"
  },
  "loadDialog": {
    "title": "加载工作流",
    "noProject": "请先选择项目目录",
    "loadFailed": "加载工作流列表失败"
  },
  "validation": {
    "failed": "验证失败",
    "missingEndNode": "工作流必须至少包含一个 End 节点"
  },
  "save": {
    "success": "保存成功",
    "savedTo": "已保存到 {{path}}",
    "failed": "保存失败"
  }
}
```

---

## 3. 上游同步翻译流程

> **重要**: 每次同步上游后，必须重新应用本地配置修改，因为上游仓库不含 zh-CN 支持。

### 3.0 上游同步完整检查清单

```
□ 1. 备份当前翻译目录
   └─ cp -r apps/frontend/src/shared/i18n/locales/zh-CN ~/backup-zh-CN/

□ 2. 拉取上游更新
   └─ git pull upstream main

□ 3. 检查是否有新的翻译键
   └─ node scripts/i18n-check.js

□ 4. 修改配置文件（⚠️ 必做，上游会覆盖）
   ├─ apps/frontend/src/shared/constants/i18n.ts
   │  └─ 添加 'zh-CN' 到 SupportedLanguage 和 AVAILABLE_LANGUAGES
   └─ apps/frontend/src/shared/i18n/index.ts
      ├─ 添加 zh-CN 翻译资源导入
      ├─ 添加 'zh-CN' 到 resources
      └─ 确保 ns 数组包含所有命名空间

□ 5. 合并/添加翻译文件
   ├─ 恢复备份的 zh-CN 翻译（如果有冲突）
   └─ 添加新增键的翻译

□ 6. 验证
   ├─ node scripts/i18n-check.js
   ├─ npm run build:frontend
   └─ 启动应用测试语言切换

□ 7. 提交
   └─ git commit -m "chore: 更新中文翻译支持"
```

### 3.1 同步场景

#### 场景 1: 主应用 (frontend) 有新翻译

```bash
# 1. 拉取上游更新
git pull upstream main

# 2. 检查是否有新的翻译键
node scripts/i18n-check.js

# 3. 将新增的键翻译到中文
# 编辑 apps/frontend/src/shared/i18n/locales/zh-CN/*.json

# 4. 验证翻译完整性
node scripts/i18n-check.js
```

#### 场景 2: CC-WF-Studio 有新翻译

```bash
# 1. 检查是否有新的翻译键
对比 apps/cc-wf-studio/src/i18n/locales/en/workflowStudio.json

# 2. 将新增的键翻译到所有语言
# 编辑 apps/cc-wf-studio/src/i18n/locales/*/workflowStudio.json

# 3. 同步到主应用的 workflowStudio.json（如果需要）
```

### 3.2 翻译检查脚本

运行以下命令检查翻译完整性：

```bash
# 检查所有翻译
node scripts/i18n-check.js

# 输出示例:
# ✅ common.json - 正常
# ✅ navigation.json - 正常
# ⚠️  发现 5 处硬编码中文:
#    apps/cc-wf-studio/src/components/toolbar/Toolbar.tsx:62
#    ...
```

### 3.3 配置文件修改（上游同步必做）

上游仓库默认不支持 zh-CN，每次同步上游后需要修改以下配置文件：

#### 文件 1: `apps/frontend/src/shared/constants/i18n.ts`

```typescript
// 修改前（上游默认）
export type SupportedLanguage = 'en' | 'fr';

export const AVAILABLE_LANGUAGES = [
  { value: 'en' as const, label: 'English', nativeLabel: 'English' },
  { value: 'fr' as const, label: 'French', nativeLabel: 'Français' }
] as const;

// 修改后（添加 zh-CN）
export type SupportedLanguage = 'en' | 'fr' | 'zh-CN';

export const AVAILABLE_LANGUAGES = [
  { value: 'en' as const, label: 'English', nativeLabel: 'English' },
  { value: 'fr' as const, label: 'French', nativeLabel: 'Français' },
  { value: 'zh-CN' as const, label: 'Chinese (Simplified)', nativeLabel: '简体中文' }
] as const;
```

#### 文件 2: `apps/frontend/src/shared/i18n/index.ts`

需要添加三部分内容：

**1. 导入中文翻译资源**（约第 24-36 行）：
```typescript
// Import Chinese (Simplified) translation resources
import zhCnCommon from './locales/zh-CN/common.json';
import zhCnNavigation from './locales/zh-CN/navigation.json';
import zhCnSettings from './locales/zh-CN/settings.json';
import zhCnTasks from './locales/zh-CN/tasks.json';
import zhCnWelcome from './locales/zh-CN/welcome.json';
import zhCnOnboarding from './locales/zh-CN/onboarding.json';
import zhCnDialogs from './locales/zh-CN/dialogs.json';
import zhCnGitlab from './locales/zh-CN/gitlab.json';
import zhCnTaskReview from './locales/zh-CN/taskReview.json';
import zhCnTerminal from './locales/zh-CN/terminal.json';
import zhCnErrors from './locales/zh-CN/errors.json';
import zhCnWorkflowStudio from './locales/zh-CN/workflowStudio.json';
```

**2. 添加到 resources 对象**（约第 60-80 行）：
```typescript
'zh-CN': {
  common: zhCnCommon,
  navigation: zhCnNavigation,
  settings: zhCnSettings,
  tasks: zhCnTasks,
  welcome: zhCnWelcome,
  onboarding: zhCnOnboarding,
  dialogs: zhCnDialogs,
  gitlab: zhCnGitlab,
  taskReview: zhCnTaskReview,
  terminal: zhCnTerminal,
  errors: zhCnErrors,
  workflowStudio: zhCnWorkflowStudio
}
```

**3. 添加到 ns 命名空间数组**：
```typescript
ns: ['common', 'navigation', 'settings', 'tasks', 'welcome', 'onboarding', 'dialogs', 'gitlab', 'taskReview', 'terminal', 'errors', 'workflowStudio'],
```

#### 快速验证

```bash
# 修改后验证 TypeScript 编译
npm run build:frontend

# 启动应用检查语言选择
npm run dev
# 进入 Settings → Language，应该能看到 "简体中文" 选项
```

### 3.4 同步 CC-WF-Studio 翻译到各语言

当英文翻译更新后，需要同步到其他所有语言：

```bash
# 1. 获取新增的翻译键
# 2. 翻译到各语言

# 日语 (ja)
ja: {
  "toolbar": {
    "save": "保存",
    "saving": "保存中..."
  }
}

# 韩语 (ko)
ko: {
  "toolbar": {
    "save": "저장",
    "saving": "저장 중..."
  }
}

# 繁体中文 (zh-TW)
zh-TW: {
  "toolbar": {
    "save": "儲存",
    "saving": "儲存中..."
  }
}
```

---

## 4. 翻译代码规范

### 4.1 组件中使用翻译

```typescript
// ✅ 正确 - 使用翻译
import { useTranslation } from 'react-i18next';

const { t } = useTranslation('ccwfstudio');

<Button>{t('toolbar.save')}</Button>

// ❌ 错误 - 硬编码中文
<Button>保存</Button>
```

### 4.2 变量命名规范

| 变量名 | 用途 | 示例 |
|--------|------|------|
| `{{path}}` | 文件路径 | "当前项目: {{path}}" |
| `{{name}}` | 名称 | "工作流: {{name}}" |
| `{{count}}` | 数量 | "已选择 {{count}} 个文件" |
| `{{current}}` | 当前值 | "第 {{current}} 步" |
| `{{total}}` | 总数 | "共 {{total}} 步" |

### 4.3 翻译规范

| English | 简体中文 | 繁体中文 | 日本語 | 한국어 |
|---------|---------|---------|--------|--------|
| Save | 保存 | 儲存 | 保存 | 저장 |
| Load | 加载 | 載入 | 読み込み | 불러오기 |
| Export | 导出 | 匯出 | エクスポート | 내보내기 |
| Project | 项目 | 專案 | プロジェクト | 프로젝트 |
| Workflow | 工作流 | 工作流程 | ワークフロー | 워크플로우 |

### 4.4 术语标准

#### 通用界面术语

| English | 中文 | 使用场景 |
|---------|------|----------|
| Settings | 设置 | 界面设置 |
| Save | 保存 | 保存操作 |
| Cancel | 取消 | 取消操作 |
| Delete | 删除 | 删除操作 |
| Create | 创建 | 创建新项目/任务 |
| Edit | 编辑 | 编辑现有项目 |
| Update | 更新 | 更新内容 |
| Refresh | 刷新 | 刷新数据 |
| Confirm | 确认 | 确认操作 |
| Skip | 跳过 | 跳过步骤 |
| Open | 打开 | 打开项目/文件 |
| Close | 关闭 | 关闭界面/标签 |
| Start | 开始 | 开始任务 |
| Stop | 停止 | 停止操作 |
| Retry | 重试 | 重试失败操作 |

#### 技术术语

| English | 中文 | 备注 |
|---------|------|------|
| Pull Request | 拉取请求 | 简称 "PR" |
| Merge Request | 合并请求 | GitLab 专用 |
| Commit | 提交 | Git 提交 |
| Branch | 分支 | Git 分支 |
| Repository | 仓库 | Git 仓库 |
| Workflow | 工作流 | CI/CD 工作流 |
| Pipeline | 流水线 | CI/CD 流水线 |
| Terminal | 终端 | 命令行终端 |
| Worktree | worktrees | 保留英文 |
| Agent | Agent | AI 代理 |
| Kanban | 看板 | 项目管理 |
| Roadmap | 路线图 | 功能规划 |
| Authentication | 身份验证 | 登录验证 |
| Integration | 集成 | 第三方集成 |

#### 品牌名称（保持原文）

- GitHub
- GitLab
- Linear
- Claude
- Anthropic
- Auto Claude
- Claude Code
- OAuth

### 4.5 技术规范

#### 插值变量

✅ **正确**: 保留所有变量
```json
{
  "fileCount": "已选择 {{count}} 个文件",
  "userName": "欢迎，{{userName}}！"
}
```

❌ **错误**: 修改或删除变量
```json
{
  "fileCount": "已选择 3 个文件"  // 错误！硬编码了数值
}
```

#### JSON 格式

✅ **正确**: 标准格式
```json
{
  "key": "值",
  "nested": {
    "key": "嵌套值"
  }
}
```

❌ **错误**: 格式问题
```json
{
  "key": "值",  // 不要有尾随逗号
  'key': "值"   // 必须使用双引号
}
```

#### 特殊字符处理

- **引号**: JSON 字符串值中使用双引号 `"`，内部引号转义为 `\"`
- **反斜杠**: 需要转义为 `\\`
- **换行符**: 使用 `\n`

### 4.6 复数处理

中文没有语法上的复数变化，使用通用形式：

```json
// 英文（区分单复数）
{
  "file": "{{count}} file",
  "file_plural": "{{count}} files"
}

// 中文（通用形式）
{
  "file": "{{count}} 个文件"
}
```

### 4.7 时间格式

| English Pattern | 中文 Pattern |
|----------------|-------------|
| Just now | 刚刚 |
| {{count}}m ago | {{count}} 分钟前 |
| {{count}}h ago | {{count}} 小时前 |
| {{count}}d ago | {{count}} 天前 |

---

## 5. 质量保证

### 5.1 自动化验证

#### JSON 语法检查

```bash
# 验证单个文件
python3 -m json.tool path/to/file.json

# 验证所有中文翻译文件
for file in apps/frontend/src/shared/i18n/locales/zh-CN/*.json; do
  python3 -m json.tool "$file" > /dev/null && echo "✓ $file" || echo "✗ $file"
done
```

#### 键完整性检查

使用项目提供的检查脚本：

```bash
# 检查所有翻译
node scripts/i18n-check.js

# 输出示例:
# ✅ common.json - 正常
# ✅ navigation.json - 正常
# ⚠️  发现 5 处硬编码中文:
#    apps/cc-wf-studio/src/components/toolbar/Toolbar.tsx:62
```

### 5.2 手动测试清单

#### 语言切换功能

- [ ] 打开应用
- [ ] 进入 Settings → Language
- [ ] 选择 "简体中文"
- [ ] 验证 UI 立即切换为中文
- [ ] 重启应用验证设置持久化

#### UI 组件翻译

- [ ] 欢迎界面显示中文
- [ ] 导航菜单已翻译
- [ ] 看板视图已翻译
- [ ] 任务列表已翻译
- [ ] 设置界面显示中文
- [ ] 对话框消息为中文
- [ ] 错误消息为中文
- [ ] 无障碍标签正常工作

#### 插值功能测试

- [ ] 时间格式: "5 分钟前" 正确显示
- [ ] 数量: "3 个文件" 正确显示
- [ ] 名称: "项目名称" 正确显示
- [ ] 所有变量正确替换

#### UI 显示测试

- [ ] 文本无溢出
- [ ] 文本无截断
- [ ] 按钮标签完整显示
- [ ] 菜单项文字清晰
- [ ] 布局正常无错位

#### 术语测试

- [ ] 术语使用一致
- [ ] 技术术语处理正确
- [ ] 品牌名称保持原文
- [ ] 缩写词适当使用

---

## 6. 维护指南

### 6.1 添加新翻译

#### 场景 1: 添加新功能翻译

**步骤**:

1. **在英文文件中添加新键**
   ```json
   // apps/frontend/src/shared/i18n/locales/en/common.json
   {
     "newFeature": {
       "title": "New Feature",
       "description": "Description of the new feature"
     }
   }
   ```

2. **在中文文件中添加对应翻译**
   ```json
   // apps/frontend/src/shared/i18n/locales/zh-CN/common.json
   {
     "newFeature": {
       "title": "新功能",
       "description": "新功能的描述"
     }
   }
   ```

3. **在组件中使用**
   ```typescript
   const { t } = useTranslation('common');
   <h3>{t('newFeature.title')}</h3>
   <p>{t('newFeature.description')}</p>
   ```

#### 场景 2: 添加带变量的翻译

**步骤**:

1. **定义带插值变量的键**
   ```json
   // en/common.json
   {
     "userGreeting": "Welcome, {{userName}}!"
   }
   ```

2. **添加中文翻译（保留变量名）**
   ```json
   // zh-CN/common.json
   {
     "userGreeting": "欢迎，{{userName}}！"
   }
   ```

3. **使用时传入变量值**
   ```typescript
   t('userGreeting', { userName: '张三' })
   // 输出: "欢迎，张三！"
   ```

### 6.2 更新现有翻译

**更新流程**:

1. **识别需要更新的文件**
   - 确定哪个命名空间包含需要更新的键

2. **修改英文源文件**
   - 在 `en/` 目录下更新键值

3. **同步更新中文翻译**
   - 在 `zh-CN/` 目录下更新对应键值
   - 确保键名完全一致

4. **验证格式**
   ```bash
   # 验证 JSON 语法
   python3 -m json.tool apps/frontend/src/shared/i18n/locales/zh-CN/common.json
   ```

5. **测试显示效果**
   - 启动应用
   - 切换到中文语言
   - 验证修改的翻译正确显示

### 6.3 最佳实践

#### 开发阶段

1. **添加新功能时同步添加翻译键**
   - 在开发过程中就添加翻译键
   - 不要等到功能完成才添加

2. **使用描述性的键名**
   ```typescript
   // ✅ 好
   t('settings.apiProfiles.addProfile')
   
   // ❌ 差
   t('settings.button1')
   ```

3. **提供足够的上下文**
   - 对于模糊的文本，添加注释说明使用场景
   - 在英文文件中添加 translator notes

#### 审查阶段

1. **检查翻译完整性**
   - 确保所有新键都有中文翻译
   - 验证没有遗漏的命名空间

2. **验证插值变量**
   - 确保所有变量都正确保留
   - 测试变量替换功能

3. **UI 显示测试**
   - 在实际应用中测试翻译效果
   - 检查文本是否被截断
   - 确认布局正常

---

## 7. 常见问题

### Q1: 如何处理复数形式？

**中文处理方式**: 中文没有语法上的复数变化，使用通用形式：

```json
// 英文（区分单复数）
{
  "file": "{{count}} file",
  "file_plural": "{{count}} files"
}

// 中文（通用形式）
{
  "file": "{{count}} 个文件"
}
```

### Q2: 如何处理非常长的文本？

**策略**:
1. 简化表达（中文通常更简洁）
2. 使用缩写（在保持清晰的前提下）
3. 如需换行，在 JSON 中使用 `\n`
4. 必要时调整 UI 组件布局

### Q3: 如何翻译技术术语？

**决策树**:
```
是否为通用开发者术语？
├─ 是 → 翻译为标准中文术语
│   ├─ 例: "Settings" → "设置"
│   └─ 例: "Terminal" → "终端"
└─ 否 → 保留英文
    ├─ 例: "OAuth" → "OAuth"
    ├─ 例: "API" → "API"
    └─ 例: "CLI" → "CLI"
```

### Q4: 如何保持术语一致性？

**最佳实践**:
1. 维护术语表（见"术语标准"部分）
2. 参考现有翻译
3. 使用搜索功能查找已翻译的类似术语
4. 与团队讨论新术语的翻译

### Q5: 如何翻译品牌名称？

**规则**: 所有品牌名称保持原文

**示例**:
- GitHub → GitHub
- GitLab → GitLab
- Linear → Linear
- Claude Code → Claude Code
- Auto Claude → Auto Claude

### Q6: 翻译文件出现乱码怎么办？

**解决方案**:
1. 确认文件保存为 UTF-8 编码
2. 检查 JSON 文件不包含 BOM
3. 使用文本编辑器确认编码正确

### Q7: 如何处理文化差异？

**原则**:
1. 使用符合中国用户习惯的表达
2. 避免直译导致的生硬表达
3. 考虑中文用户的使用场景
4. 必要时添加解释性文字

**示例**:
```json
// 英文
{
  "yoloMode": "YOLO Mode (execute without confirmation)"
}

// 中文（添加解释）
{
  "yoloMode": "YOLO 模式（跳过确认直接执行）"
}
```

---

## 附录

### A. 翻译文件统计

#### 主应用 (apps/frontend) - 文件详情

| # | 文件名 | 键数 | 复杂度 |
|---|--------|------|--------|
| 1 | errors.json | 1 | 简单 |
| 2 | welcome.json | 8 | 简单 |
| 3 | terminal.json | 37 | 中等 |
| 4 | navigation.json | 61 | 中等 |
| 5 | taskReview.json | ~30 | 中等 |
| 6 | dialogs.json | ~120 | 中等 |
| 7 | gitlab.json | ~100 | 中等 |
| 8 | onboarding.json | ~120 | 复杂 |
| 9 | tasks.json | ~95 | 复杂 |
| 10 | common.json | ~220 | 复杂 |
| 11 | settings.json | ~210 | 最复杂 |
| **总计** | **~1002** | - |

#### 翻译键数量分布

```
总翻译键: ~1002 个

按文件分布:
├── common.json:     ~220 键 (22%)
├── settings.json:    ~210 键 (21%)
├── dialogs.json:     ~120 键 (12%)
├── onboarding.json:  ~120 键 (12%)
├── gitlab.json:      ~100 键 (10%)
├── tasks.json:       ~95 键  (9%)
├── navigation.json:   ~61 键  (6%)
├── terminal.json:    ~37 键  (4%)
├── taskReview.json:  ~30 键  (3%)
├── welcome.json:      ~8 键   (1%)
└── errors.json:      ~1 键   (0%)
```

#### 文本类型分布

```
UI 元素文本:     ~60%
错误消息:         ~5%
帮助文本:        ~15%
无障碍标签:      ~15%
状态指示:         ~5%
```

#### 主应用 (apps/frontend)

```
apps/frontend/src/shared/i18n/locales/
├── en/
│   ├── common.json        # ~220 键
│   ├── navigation.json    # ~61 键
│   ├── settings.json      # ~210 键
│   ├── tasks.json         # ~95 键
│   ├── welcome.json       # ~8 键
│   ├── onboarding.json    # ~120 键
│   ├── dialogs.json       # ~120 键
│   ├── gitlab.json        # ~100 键
│   ├── taskReview.json    # ~30 键
│   ├── terminal.json      # ~37 键
│   └── errors.json        # ~1 键
├── fr/
│   └── [相同结构]
└── zh-CN/
    └── [相同结构]
```

#### CC-WF-Studio (apps/cc-wf-studio)

```
apps/cc-wf-studio/src/i18n/locales/
├── en/workflowStudio.json    # 英文 (~656 键)
├── fr/workflowStudio.json    # 法文 (~656 键)
├── zh-CN/workflowStudio.json # 简体中文 (~656 键)
├── ja/workflowStudio.json    # 日语 (~656 键)
├── ko/workflowStudio.json    # 韩语 (~656 键)
└── zh-TW/workflowStudio.json # 繁体中文 (~656 键)
```

### B. 本地翻译提交记录

| 日期 | 提交 | 说明 |
|------|------|------|
| 2025-01-15 | (前端翻译) | 完成主应用 11 个 JSON 文件翻译 (~1002 键) |
| 2025-01-15 | (配置更新) | 添加 zh-CN 到 i18n.ts 和 index.ts |
| 2025-01-27 | (CC-WF-Studio) | 完成 6 语言翻译 (en/fr/zh-CN/ja/ko/zh-TW) |
| 2025-01-27 | (指南文档) | 完成 README、linux、windows 中文翻译 |
| 2025-01-27 | (CLA) | 完成 CLA 中文翻译 |
| 2025-01-27 | (Toolbar) | 更新 Toolbar.tsx 使用翻译系统（移除硬编码中文） |
| 2025-01-27 | (LoadWorkflowDialog) | 更新 LoadWorkflowDialog.tsx 使用翻译系统 |

### C. 相关文档

| 文档 | 用途 | 说明 |
|------|------|------|
| `翻译进度.md` | 进度跟踪 | 跟踪各模块翻译完成状态 |
| `README.zh-CN.md` | 项目说明 | Auto-Claude 中文项目介绍 |
| `CLI-USAGE.zh-CN.md` | CLI 使用指南 | 命令行工具使用说明 |
| `CONTRIBUTING.zh-CN.md` | 贡献指南 | 如何参与项目贡献 |
| `scripts/i18n-check.js` | 翻译检查 | 自动检查翻译完整性的脚本 |

> **注**: `Auto-Claude简体中文翻译完整文档.md` 的内容已合并到本文档（v3.1），可删除原文件。

**文档入口**：本文档 (**中文翻译完整指南.md**) 是翻译工作的主要参考文档。

### D. 快速命令

```bash
# 检查翻译完整性
node scripts/i18n-check.js

# 验证 JSON 格式
for file in apps/cc-wf-studio/src/i18n/locales/*/*.json; do
  python3 -m json.tool "$file" > /dev/null && echo "✓ $file" || echo "✗ $file"
done

# 检查硬编码中文
grep -r "[\u4e00-\u9fff]" apps/cc-wf-studio/src/components/
```

---

**文档版本**: 3.4
**最后更新**: 2025-01-27
**维护者**: Auto-Claude 团队
